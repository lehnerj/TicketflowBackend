<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">

    <Properties>
        <Property name="LOG_PATTERN">
            %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %logger{36} iteration=%X{iteration} traceId=%X{traceId} – %msg%n
        </Property>
    </Properties>

    <Appenders>
        <!-- Console – structured, one line per log event; Instana agent picks this up -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>
    </Appenders>

    <Loggers>
        <!-- Application loggers at DEBUG so every trace hop is visible -->
        <Logger name="com.ibm.ticketflow" level="DEBUG" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>

        <!-- Spring and Cassandra driver at INFO to reduce noise -->
        <Logger name="org.springframework"           level="INFO"  additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>
        <Logger name="com.datastax.oss.driver"       level="INFO"  additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>

        <Root level="WARN">
            <AppenderRef ref="Console"/>
        </Root>
    </Loggers>

</Configuration>
