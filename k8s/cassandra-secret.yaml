apiVersion: v1
kind: Secret
metadata:
  name: cassandra-secret
  namespace: ticketflow
type: Opaque
stringData:
  # Override these values with your real credentials before applying
  username: cassandra
  password: cassandra
