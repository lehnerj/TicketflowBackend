# ServiceAccount for the Cassandra StatefulSet.
# A dedicated SA (rather than using "default") means the anyuid SCC grant
# is scoped only to Cassandra pods and does not affect any other workload
# in the ticketflow namespace.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: cassandra-sa
  namespace: ticketflow
